apiVersion: v1
kind: Service
metadata:
  name: op-scim-bridge
  labels:
    app: op-scim-bridge
spec:
  type: NodePort
  ports:
    - port: 3002
      targetPort: http
  selector:
    app: op-scim-bridge
